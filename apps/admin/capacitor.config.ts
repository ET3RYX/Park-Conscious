import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parkconscious.admin',
  appName: 'ParkConsciousAdmin',
  webDir: 'dist'
};

export default config;
