{
  "name": "park-conscious",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp *.html favicon.ico favicon.png public/ 2>/dev/null || true && cp -r assets scripts styles public/ 2>/dev/null || true && cd Events && npm install && npm run build && cd .. && cd AdminPanel && npm install && npm run build",
    "dev:backend": "MONGODB_URI='mongodb+srv://piyush:parkconscious@cluster0.23idw3z.mongodb.net/park-conscious?appName=Cluster0' JWT_SECRET='hellothisispiyush' node local-server.js"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "bcryptjs": "^2.4.3",
    "busboy": "^1.6.0",
    "cloudinary": "^2.9.0",
    "cookie": "^1.1.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "8.10.1"
  }
}
